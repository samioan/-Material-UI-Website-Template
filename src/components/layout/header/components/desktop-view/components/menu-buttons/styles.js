import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  menuButton: {
    fontWeight: 700,
    size: 18,
    marginLeft: 38,
    color: "inherit",
  },
}));

export { styles };
export default styles;
