const styles = () => ({
  modal: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "column",
  },
  container: {
    backgroundColor: "#000000",
    borderRadius: 8,
    maxWidth: 600,
  },
});

export { styles };
export default styles;
