import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  logo: {
    fontWeight: 600,
    color: "#fff",
    textAlign: "left",
  },
}));

export { styles };
export default styles;
