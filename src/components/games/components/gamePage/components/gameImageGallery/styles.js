import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  gallery: {
    paddingTop: 8,
  },
  imageContainer: {
    padding: 8,
  },
  image: {
    width: "100%",
  },
}));

export { styles };
export default styles;
