import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  buttonsContainer: {
    paddingTop: 16,
    paddingBottom: 16,
  },
  button: {
    margin: 8,
    width: 150,
  },
}));

export { styles };
export default styles;
